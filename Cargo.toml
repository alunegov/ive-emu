[package]
name = "ive-emu"
version = "0.2.2"
edition = "2021"

[dependencies]
tokio = { version = "1.0", default-features = false, features = ["full"] }
tokio-modbus = { version = "0.11", default-features = false, features = ["rtu-server"] }
tokio-serial = "5.0"
